usage: camera_example1_callback -u <camera_unit>

Example demonstrating processing of camera data received by a callback

    options:
        -u:  Camera unit to use; if not specified, will list available units and exit
